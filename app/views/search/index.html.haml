%span
  type "c" for example
#placeholder

%span
  custom render
#custom

%span
  type some value that does not exists
#customNoItems


:javascript
  $(function() {
    window.router = new Backcomplete.Routers.SearchRouter({results: #{@results.to_json()}});
    Backbone.history.start();
  });